# 변경 diff — 산 배치 재설계(클러스터형)

- 일시: 2026-09-06 / 베이스: 2f0240a
- 대상: index.html(1곳)
- 균일 링(22)+내륙 산발적(28) → 산맥 클러스터 13곳(불규칙 간격≥300), 산맥당 2~6봉우리
- 회피(rejMB)가 산 반지름(rw)을 버퍼로 반영 → 큰 산도 길·마을 밑동까지 침범 안 함(시뮬레이션 검증: 침범 0/50)
- 크기 다양화(반지름 50~230, 높이 70~260)로 획일함 해소
- 산끼리 최소 간격((r1+r2)*0.62) 유지 → 뭉개져 붙는 덩어리 방지, 클러스터 사이는 넓은 평지
- 검증: node --check 통과 / jsdom "no script errors"(기존 FAIL 3개만) / Node 시뮬레이션으로 배치 로직 별도 검증
